# Optional — used as HTTP-Referer header for OpenRouter.
# Set this in Vercel project settings to your production URL.
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# This app uses BYOK (Bring Your Own Key).
# API keys are entered in Settings and stored in the browser only.
# Do NOT add API keys here for production deployment.
